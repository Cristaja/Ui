Ошибка возникла из-за попытки записать файл с тем же именем, что и уже существующий (дублирование вызова скрипта или совпадение меток времени до секунды). GitHub API требует параметр sha для обновления существующего файла, а мы создаём новый. Чтобы избежать этого, я сделаю имена файлов по-настоящему уникальными (с миллисекундами и случайным компонентом), а также добавлю защиту от повторной обработки одного ответа.Обновлённый скрипт (уникальные имена, защита от дубликатов)javascriptCopyDownload// ==UserScript==
// @name         DeepSeek → Cristaja/Ui (Stable)
// @namespace    http://tampermonkey.net/
// @version      5.6
// @description  Сохраняет ответы в репозиторий (уникальные имена, без дублей)
// @match        https://chat.deepseek.com/*
// @grant        GM_xmlhttpRequest
// @connect      api.github.com
// ==/UserScript==

(function() {
    'use strict';

    const GITHUB_TOKEN = '[POTENTIAL_SECRET_REMOVED]';
    const REPO_OWNER   = 'Cristaja';
    const REPO_NAME    = 'Ui';
    const BRANCH       = 'main';
    const BASE_FOLDER  = 'answers/';

    const processed = new WeakSet();

    function sanitizeText(text) {
        text = text.replace(/ghp_[a-zA-Z0-9]{36,}/g, '[GITHUB_TOKEN_REMOVED]');
        text = text.replace(/\b[a-zA-Z0-9_-]{30,}\b/g, (match) => {
            if (/^(ghp_|github_pat_|sk-|key-|api-|sec-|token-)/i.test(match) ||
                (match.length > 35 && /[A-Z]/.test(match) && /[a-z]/.test(match) && /\d/.test(match))) {
                return '[POTENTIAL_SECRET_REMOVED]';
            }
            return match;
        });
        return text;
    }

    function getFinalAnswerText(container) {
        const blocks = container.querySelectorAll('.ds-markdown');
        if (blocks.length === 0) return null;
        const lastBlock = blocks[blocks.length - 1];
        const clone = lastBlock.cloneNode(true);
        clone.querySelectorAll('details').forEach(d => d.remove());
        const rawText = clone.textContent?.trim();
        return rawText ? sanitizeText(rawText) : null;
    }

    function generateUniqueFileName() {
        const now = new Date();
        const pad = (n) => String(n).padStart(2, '0');
        // Добавляем миллисекунды и случайное число для абсолютной уникальности
        const ms = String(now.getMilliseconds()).padStart(3, '0');
        const random = Math.random().toString(36).substring(2, 6); // 4 символа
        const timestamp = `${now.getFullYear()}-${pad(now.getMonth()+1)}-${pad(now.getDate())}_${pad(now.getHours())}-${pad(now.getMinutes())}-${pad(now.getSeconds())}`;
        return `answer_${timestamp}_${ms}_${random}.md`;
    }

    function commitAnswerFile(text) {
        const filePath = BASE_FOLDER + generateUniqueFileName();
        const contentBase64 = btoa(unescape(encodeURIComponent(text)));
        const url = `https://api.github.com/repos/${REPO_OWNER}/${REPO_NAME}/contents/${filePath}`;

        GM_xmlhttpRequest({
            method: 'PUT',
            url: url,
            headers: {
                'Authorization': `token ${GITHUB_TOKEN}`,
                'Accept': 'application/vnd.github.v3+json',
                'Content-Type': 'application/json'
            },
            data: JSON.stringify({
                message: `Add DeepSeek answer [${filePath}]`,
                content: contentBase64,
                branch: BRANCH
            }),
            onload: function(response) {
                if (response.status === 201 || response.status === 200) {
                    const data = JSON.parse(response.responseText);
                    console.log('📁 Сохранён:', data.content.html_url);
                } else if (response.status === 422) {
                    const err = JSON.parse(response.responseText);
                    if (err.message?.includes('sha')) {
                        console.warn('⚠️ Файл с таким именем уже существует, пробую другое имя...');
                        // Маловероятно, но если случилось — повторяем с новым именем
                        commitAnswerFile(text);
                    } else {
                        console.error('❌ Ошибка (422):', err.message);
                    }
                } else {
                    console.error('❌ Ошибка:', response.responseText);
                }
            },
            onerror: (err) => console.error('❌ Сеть:', err)
        });
    }

    function processAnswer(container) {
        const text = getFinalAnswerText(container);
        if (!text) return false;
        commitAnswerFile(text);
        return true;
    }

    function handleContainer(container) {
        if (processed.has(container)) return;
        processed.add(container);
        // Небольшая задержка, чтобы избежать срабатывания до полной отрисовки текста
        setTimeout(() => {
            if (processAnswer(container)) return;
            const obs = new MutationObserver(() => {
                if (processAnswer(container)) obs.disconnect();
            });
            obs.observe(container, { childList: true, subtree: true });
            setTimeout(() => obs.disconnect(), 30000);
        }, 500);
    }

    function handleNode(node) {
        const msg = node.closest?.('.ds-message');
        if (msg) handleContainer(msg);
    }

    const mainObs = new MutationObserver(muts => {
        for (const m of muts) {
            for (const node of m.addedNodes) {
                if (node.nodeType !== Node.ELEMENT_NODE) continue;
                if (node.matches?.('.ds-message')) handleContainer(node);
                if (node.querySelectorAll) {
                    if (node.querySelectorAll('.ds-markdown, button').length > 0) handleNode(node);
                    node.querySelectorAll('.ds-message').forEach(handleContainer);
                }
            }
        }
    });

    mainObs.observe(document.body, { childList: true, subtree: true });
    document.querySelectorAll('.ds-message').forEach(handleContainer);
    console.log('🤖 Сохранение ответов (стабильное) запущено.');
})();Что улучшеноУникальность имени: добавил миллисекунды и случайные символы. Теперь совпадение практически невозможно.Защита от дубликатов: WeakSet предотвращает повторную обработку одного контейнера, а задержка 500 мс даёт тексту полностью отрендериться.Обработка ошибки SHA: если вдруг файл с именем существует (немыслимо, но вдруг), скрипт автоматически повторит попытку с новым именем.Действия сейчасЗамените ваш текущий скрипт на этот (сохраните).Задайте любой вопрос и дождитесь ответа.Проверьте консоль — должно появиться 📁 Сохранён: ... без ошибок.Загляните в папку answers/ репозитория — там появится новый файл.Если ошибка с sha была единичным сбоем, сейчас она не повторится. Если что-то пойдёт не так — пришлите новый лог, я оперативно помогу.