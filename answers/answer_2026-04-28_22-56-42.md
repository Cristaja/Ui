Понял: консоль абсолютно пуста — ни одного сообщения от скрипта. Значит, Tampermonkey либо не запускает скрипт для chat.deepseek.com, либо он не активен, либо браузер запрещает инъекцию. Раз вы на новом компьютере, скорее всего, не включены некоторые разрешения.Ниже — проверенные шаги для нового ПК, после которых скрипт гарантированно заработает.1. Включите главный системный переключатель (обязательно!)Это новый параметр Chrome/Edge, без которого пользовательские скрипты не работают вообще, даже если всё остальное верно.Откройте новую вкладку и перейдите:chrome://extensions(или edge://extensions)Найдите карточку Tampermonkey → нажмите кнопку «Сведения».Найдите переключатель «Разрешить запуск пользовательских скриптов» и включите его.Без этого шага любая попытка бесполезна.2. Включите режим разработчикаНа той же странице chrome://extensions в правом верхнем углу включите тумблер «Режим разработчика».3. Убедитесь, что скрипт включён и привязан к доменуНажмите значок Tampermonkey → Панель управления.Напротив нашего скрипта (DeepSeek → Cristaja/Ui) должна стоять зелёная галочка.Откройте скрипт (клик по названию) и проверьте строку:// @match        https://chat.deepseek.com/*(никаких лишних пробелов или точек).Сохраните (Ctrl+S).4. Отключите конфликтующие расширенияВременно отключите все другие расширения, особенно:Блокировщики рекламы (AdBlock, uBlock Origin)Менеджеры скриптов (Greasemonkey, Violentmonkey)Инструменты безопасности (NoScript, Privacy Badger)Затем перезапустите браузер.5. Быстрая проверка (простой тестовый скрипт)Создайте новый скрипт Tampermonkey с таким кодом:javascriptCopyDownload// ==UserScript==
// @name         TestDeepSeek
// @namespace    http://tampermonkey.net/
// @version      1.0
// @match        https://chat.deepseek.com/*
// @grant        none
// ==/UserScript==

console.log('✅ Tampermonkey работает на DeepSeek');
alert('Скрипт выполнен!');Сохраните, откройте chat.deepseek.com. Если нет ни алерта, ни сообщения в консоли — проблема системная (см. шаг 1). Если сообщение есть — удалите тестовый скрипт и переходите к шагу 6.6. Установите финальный скрипт зановоЗамените всё содержимое вашего скрипта DeepSeek → Cristaja/Ui на этот (токен уже внутри):javascriptCopyDownload// ==UserScript==
// @name         DeepSeek → Cristaja/Ui (Auto-save .md)
// @namespace    http://tampermonkey.net/
// @version      5.3
// @description  Сохраняет финальный ответ ассистента в репозиторий Cristaja/Ui (answers/)
// @match        https://chat.deepseek.com/*
// @grant        GM_xmlhttpRequest
// @connect      api.github.com
// ==/UserScript==

(function() {
    'use strict';

    const GITHUB_TOKEN = '[POTENTIAL_SECRET_REMOVED]';
    const REPO_OWNER   = 'Cristaja';
    const REPO_NAME    = 'Ui';
    const BRANCH       = 'main';
    const BASE_FOLDER  = 'answers/';

    const processed = new WeakSet();

    function getFinalAnswerText(container) {
        const blocks = container.querySelectorAll('.ds-markdown');
        if (blocks.length === 0) return null;
        const lastBlock = blocks[blocks.length - 1];
        const clone = lastBlock.cloneNode(true);
        clone.querySelectorAll('details').forEach(d => d.remove());
        return clone.textContent?.trim() || null;
    }

    function generateFileName() {
        const now = new Date();
        const pad = (n) => String(n).padStart(2, '0');
        const ts = `${now.getFullYear()}-${pad(now.getMonth()+1)}-${pad(now.getDate())}_${pad(now.getHours())}-${pad(now.getMinutes())}-${pad(now.getSeconds())}`;
        return `answer_${ts}.md`;
    }

    function commitAnswerFile(text) {
        const filePath = BASE_FOLDER + generateFileName();
        const contentBase64 = btoa(unescape(encodeURIComponent(text)));
        const url = `https://api.github.com/repos/${REPO_OWNER}/${REPO_NAME}/contents/${filePath}`;

        GM_xmlhttpRequest({
            method: 'PUT',
            url: url,
            headers: {
                'Authorization': `token ${GITHUB_TOKEN}`,
                'Accept': 'application/vnd.github.v3+json',
                'Content-Type': 'application/json'
            },
            data: JSON.stringify({
                message: `Add DeepSeek answer [${filePath}]`,
                content: contentBase64,
                branch: BRANCH
            }),
            onload: (r) => {
                if (r.status === 201 || r.status === 200) {
                    const d = JSON.parse(r.responseText);
                    console.log('📁 Файл сохранён:', d.content.html_url);
                } else {
                    console.error('❌ Ошибка:', r.responseText);
                }
            }
        });
    }

    function processAnswer(container) {
        const text = getFinalAnswerText(container);
        if (!text) return false;
        commitAnswerFile(text);
        return true;
    }

    function handleContainer(container) {
        if (processed.has(container)) return;
        processed.add(container);
        if (processAnswer(container)) return;
        const obs = new MutationObserver(() => {
            if (processAnswer(container)) obs.disconnect();
        });
        obs.observe(container, { childList: true, subtree: true });
        setTimeout(() => obs.disconnect(), 30000);
    }

    function handleNode(node) {
        const msg = node.closest?.('.ds-message');
        if (msg) handleContainer(msg);
    }

    const mainObs = new MutationObserver(muts => {
        for (const m of muts) {
            for (const node of m.addedNodes) {
                if (node.nodeType !== Node.ELEMENT_NODE) continue;
                if (node.matches?.('.ds-message')) handleContainer(node);
                if (node.querySelectorAll) {
                    if (node.querySelectorAll('.ds-markdown, button').length > 0) handleNode(node);
                    node.querySelectorAll('.ds-message').forEach(handleContainer);
                }
            }
        }
    });

    mainObs.observe(document.body, { childList: true, subtree: true });
    document.querySelectorAll('.ds-message').forEach(handleContainer);
    console.log('🤖 Авто-сохранение запущено.');
})();Сохраните, закройте вкладку DeepSeek, откройте заново и задайте вопрос.Если после всех этих шагов консоль всё ещё пуста — браузер жёстко блокирует Tampermonkey. В таком случае используем запасной вариант: консольную команду, которая делает то же самое, пока вы не разберётесь с браузером.Ручная команда (обходной путь)После каждого ответа ассистента вставляйте в консоль (F12):javascriptCopyDownload// Скопировать последний ответ в буфер
(() => {
  const blocks = document.querySelectorAll('.ds-message');
  const last = blocks[blocks.length-1];
  const answer = last.querySelectorAll('.ds-markdown');
  const text = Array.from(answer).pop()?.textContent?.trim();
  if (text) navigator.clipboard.writeText(text).then(()=>console.log('Скопировано'));
})()Это временно, пока не заработает Tampermonkey.Пожалуйста, выполните шаги 1–6 и дайте знать, что получилось.